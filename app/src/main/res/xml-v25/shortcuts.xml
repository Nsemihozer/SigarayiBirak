<shortcuts xmlns:android="http://schemas.android.com/apk/res/android">
    <shortcut
        android:shortcutId="daily"
        android:enabled="true"
        android:icon="@drawable/addcraving"
        android:shortcutShortLabel="@string/daily_shortcut_short_label"
        android:shortcutLongLabel="@string/daily_shortcut_long_label"
        android:shortcutDisabledMessage="@string/daily_disabled_message">
        <intent
            android:action="android.intent.action.VIEW"
            android:targetPackage="com.cotyoragames.sigarayibirak"
            android:targetClass="com.cotyoragames.sigarayibirak.Desires" />
        <!-- If your shortcut is associated with multiple intents, include them
             here. The last intent in the list determines what the user sees when
             they launch this shortcut. -->
        <categories android:name="android.shortcut.conversation" />
    </shortcut>
    <!-- Specify more shortcuts here. -->
</shortcuts>